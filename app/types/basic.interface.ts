import { LanguageEnum } from "./language.enum"

export interface BasicAbilities {
    pullUp: string
    pushUp: string
    dips: string
    weight: string
    height: string
    language: LanguageEnum 
    age: string
    injury: string
}